spring:
  cloud:
    bus:
      refresh:
        enabled: true
      env:
        enabled: true

  kafka:
    bootstrap-servers:  localhost:9092


management:
  tracing:
    sampling:
      probability: 0.1
  endpoints:
    web:
      exposure:
        include: "*"

service:
  name: department-17

server:
  port: 8091
